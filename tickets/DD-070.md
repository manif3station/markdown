# DD-070

## Title

Add A0-A10, B0-B10, C0-C7, DL, and ANSI paper sizes.

## Method

TDD

## Acceptance

- `markdown` accepts ISO `A0` through `A10`
- `markdown` accepts ISO `B0` through `B10`
- `markdown` accepts ISO `C0` through `C7`
- `markdown` accepts `DL`
- `markdown` accepts `ANSI-A` through `ANSI-E`
- `-A` shorthand supports `0` through `10`
- invalid paper names still fail clearly
- Docker tests and 100% coverage pass
- installed runtime proof, docs, commit, and push are completed

## Status

Done
