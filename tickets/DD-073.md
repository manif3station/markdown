# DD-073

## Title

Add `.docx -> .md` and `.md -> .docx` routes to `dashboard markdown.convert`.

## Method

TDD

## Acceptance

- `dashboard markdown.convert report.docx` defaults to `report.md`
- `dashboard markdown.convert report.docx report.md` works through extension-driven route selection
- `dashboard markdown.convert notes.md notes.docx` works through extension-driven route selection
- the implementation reuses the existing office-document PDF backends instead of introducing a separate DOCX markdown stack
- Docker tests and `100%` statement and subroutine coverage pass
- docs, changelog, commit, and push are completed

## Status

Done
