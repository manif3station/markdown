# EPIC-037

## Title

Move `markdown` to an all-Perl conversion stack.

## Goal

Remove the remaining system-package converters so `markdown` handles markdown, html, and pdf routes with Perl modules only.
