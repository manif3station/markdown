# DD-067

## Title

Add `--paper`, `--landscape`, and `--portrait` to `markdown.convert`.

## Method

TDD

## Acceptance

- `dashboard markdown.convert source.md target.pdf --paper A3` selects A3 output
- `dashboard markdown.convert source.md target.pdf -A 3` selects the same A3 output through shorthand
- `--landscape` and `--portrait` control the PDF orientation, with portrait as the default
- invalid paper names, conflicting orientation flags, and duplicate paper-size selectors fail clearly
- non-PDF routes reject PDF-only layout flags clearly
- Docker tests and 100% coverage pass
- installed runtime proof, docs, commit, and push are completed

## Status

Done
