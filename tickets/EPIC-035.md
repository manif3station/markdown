# EPIC-035

## Title

Fix macOS package compatibility for `markdown` pdf output.

## Goal

Replace the broken Homebrew pdf package path with a supported backend and prove `markdown.convert` still works across Ubuntu and macOS expectations.
