# DD-064

## Title

Replace external document converters with CPAN modules.

## Method

TDD

## Acceptance

- `markdown` uses Perl modules for `md <-> html`
- `markdown` uses Perl modules for `md <-> pdf`
- `aptfile` and `brewfile` are no longer required for the skill
- a `cpanfile` declares the module dependencies
- Docker tests and 100% coverage pass
- installed runtime proof, docs, commit, and push are completed

## Status

Done
