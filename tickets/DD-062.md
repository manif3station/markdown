# DD-062

## Title

Add a supported macOS pdf backend for `markdown.convert`.

## Method

TDD

## Acceptance

- `dashboard skills install markdown` no longer fails on macOS because of an unavailable Homebrew package
- `markdown.convert` can generate pdf output with either `wkhtmltopdf` or `weasyprint`
- Ubuntu/Debian package declarations stay valid
- Docker tests and 100% coverage pass
- macOS install proof, docs, commit, and push are completed

## Status

Done
