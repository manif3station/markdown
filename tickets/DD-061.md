# DD-061

## Title

Build the first `markdown.convert` command.

## Method

TDD

## Acceptance

- the skill exposes `dashboard markdown.convert`
- markdown input converts to html or pdf
- html and pdf input convert back to markdown
- output naming follows the source basename when `--to` is omitted
- output extension is appended when `--to` omits it
- Ubuntu/Debian and macOS package declarations are present
- Docker tests and 100% coverage pass
- docs, commit, and push are completed

## Status

Done
