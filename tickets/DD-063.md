# DD-063

## Title

Replace flag-first CLI flow with positional files and progress logging.

## Method

TDD

## Acceptance

- `dashboard markdown.convert <source> [target]` works without requiring `--from` or `--to`
- the target route is inferred from source and target extensions
- html and pdf input still default to markdown output when no target path is supplied
- conversion progress is printed to `stderr` with visible step logs
- Docker tests and 100% coverage pass
- docs, commit, and push are completed

## Status

Done
