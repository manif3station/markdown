# EPIC-041

## Title

Fix real PDF layout wiring in `markdown`.

## Goal

Make the real default PDF renderer honor the selected paper size and orientation instead of only reporting them in JSON and logs.
