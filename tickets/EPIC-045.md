# EPIC-045

## Title

Add DOCX and PDF office-document conversion to the `markdown` skill.

## Status

Done

## Outcome

Extend `dashboard markdown.convert` so it can route DOCX to PDF and PDF to DOCX through the same extension-driven command contract while staying explicit about host Office dependencies on Linux, macOS, and Windows.

## Tickets

- `DD-072` Add DOCX and PDF office-document conversion to `dashboard markdown.convert`
