# DD-072

## Title

Add DOCX and PDF office-document conversion to `dashboard markdown.convert`.

## Method

TDD

## Acceptance

- `dashboard markdown.convert report.docx` defaults to `report.pdf`
- `dashboard markdown.convert report.docx report.pdf` works through the same extension-driven route selection as the existing markdown/html/pdf flows
- `dashboard markdown.convert scan.pdf scan.docx` routes PDF to DOCX explicitly
- the runtime includes Linux, macOS, and Windows backend selection for DOCX/PDF conversion
- dependency files or install automation are updated for the host tools the new route needs
- Docker tests and `100%` statement and subroutine coverage pass
- docs, changelog, commit, and push are completed

## Status

Done
