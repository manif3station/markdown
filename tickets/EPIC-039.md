# EPIC-039

## Title

Restore visual table rendering in `markdown` pdf output.

## Goal

Render markdown tables as visible table structures in generated PDFs instead of flattening them into plain text rows.
