# EPIC-038

## Title

Render markdown features instead of raw markup in `markdown`.

## Goal

Make `markdown` turn common markdown structures such as tables and inline code into real HTML and PDF output instead of showing the raw markdown markers.
